<%= form_with(model: aircraft) do |form| %>
  <% if aircraft.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(aircraft.errors.count, "error") %> prohibited this aircraft from being saved:</h2>

      <ul>
        <% aircraft.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :make, style: "display: block" %>
    <%= form.text_field :make %>
  </div>

  <div>
    <%= form.label :model, style: "display: block" %>
    <%= form.text_field :model %>
  </div>

  <div>
    <%= form.label :engine, style: "display: block" %>
    <%= form.select(:engine, [['Single', 'single'], ['Multi', 'multi']]) %>
  </div>
  

  <div>
    <%= form.submit %>
  </div>
<% end %>